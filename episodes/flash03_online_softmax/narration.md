# FlashAttention 03 — 전체를 저장하지 않고 Softmax할 수 있을까?

| 시간 | 내레이션 | 화면 |
|---|---|---|
| 00:00–00:07 | Softmax의 분모를 계산하려면 모든 score의 지수값을 더해야 합니다. | 전체 score row와 `m=max(x)`, `ℓ=Σexp(x−m)` |
| 00:07–00:14 | 하지만 모든 score를 한꺼번에 기억할 필요는 없습니다. 작은 블록으로 나누고 하나씩 볼 수 있습니다. | 세 개의 score block |
| 00:14–00:22 | 첫 블록의 최댓값을 구하고, 그 값을 기준으로 안정적인 지수합을 계산합니다. | `[1,3,2]`, `m₁=3`, `ℓ₁=1.503` |
| 00:22–00:31 | 두 번째 블록의 5는 지금까지의 3보다 큽니다. 새로운 기준 최댓값은 5가 됩니다. | `[2,5,4]`, `m: 3→5` |
| 00:31–00:42 | 최댓값이 바뀌어도 처음부터 다시 계산하지 않습니다. 이전 합을 새 기준에 맞게 작게 보정합니다. | `1.503 × e³⁻⁵ = 0.203`, Block 1 삭제 |
| 00:42–00:51 | 새 기준으로 보정한 이전 합에 두 번째 블록에서 계산한 지수합을 더하면 됩니다. | `0.203 + 1.418`, `m=5`, `ℓ=1.621` |
| 00:51–00:59 | 세 번째 블록까지 반복하면 숫자들은 사라지고 현재 최댓값과 누적 지수합만 남습니다. | Block 3가 지나가고 `m=5`, `ℓ=2.014` 유지 |
| 00:59–01:07 | 모든 score를 한 번에 본 경우와 블록을 순서대로 본 경우의 마지막 최댓값과 지수합은 같습니다. | Full scan과 Block-wise scan 비교 |
| 01:07–01:13 | Softmax에 필요한 정보는 전체 score 목록이 아니라 현재의 m과 ℓ로 계속 누적할 수 있습니다. | 긴 score row가 사라지고 두 state만 유지 |
| 01:13–01:18 | 다음에는 m과 ℓ 옆에 output state O를 더해 Attention 결과까지 tile마다 갱신해 보겠습니다. | `m, ℓ + O ?` |

## 전체 내레이션

Softmax의 분모를 계산하려면 모든 score의 지수값을 더해야 합니다.

하지만 모든 score를 한꺼번에 기억할 필요는 없습니다. 작은 블록으로 나누고 하나씩 볼 수 있습니다.

첫 블록의 최댓값을 구하고, 그 값을 기준으로 안정적인 지수합을 계산합니다.

두 번째 블록의 5는 지금까지의 3보다 큽니다. 새로운 기준 최댓값은 5가 됩니다.

최댓값이 바뀌어도 처음부터 다시 계산하지 않습니다. 이전 합을 새 기준에 맞게 작게 보정합니다.

새 기준으로 보정한 이전 합에 두 번째 블록에서 계산한 지수합을 더하면 됩니다.

세 번째 블록까지 같은 갱신을 반복하면, 숫자들은 사라지고 현재 최댓값과 누적 지수합만 남습니다.

모든 score를 한 번에 본 경우와 블록을 순서대로 본 경우의 마지막 최댓값과 지수합은 같습니다.

Softmax에 필요한 정보는 전체 score 목록이 아니라 현재의 m과 ℓ로 계속 누적할 수 있습니다.

다음에는 m과 ℓ 옆에 output state O를 더해 Attention 결과까지 tile마다 갱신해 보겠습니다.

